import { SalaryCalciPipe } from './salary-calci.pipe';

describe('SalaryCalciPipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryCalciPipe();
    expect(pipe).toBeTruthy();
  });
});
